apiVersion: shrine/v1
kind: Application
metadata:
  name: aterrizar-externaldeps
  owner: test 
spec:
  image: reg:falcon/aterrizardeps:latest
  port: 8080
  replicas: 1
  routing:
    domain: externaldeps.shrine.lab
    pathPrefix: /externaldeps
  networking:
    exposeToPlatform: false
